<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import
            alias="LP"
            type="android.view.ViewGroup.LayoutParams" />

        <variable
            name="viewModel"
            type="com.kaeonx.moneymanager.fragments.accounts.AccountEditViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/accountIconFL"
            android:layout_width="wrap_content"
            android:layout_height="120dp"
            android:layout_margin="12dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:padding="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <include
                android:id="@+id/accountIconFLInclude"
                layout="@layout/icon_transaction"
                app:iconDetail="@{viewModel.currentAccount.toIconDetail()}" />

        </FrameLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/accountNameETContainer"
            style="@style/FullWidthMaterialEditTextContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="Account Name"
            app:error_text="@{viewModel.accountNameETError}"
            app:layout_constraintTop_toBottomOf="@+id/accountIconFL"
            tools:layout_editor_absoluteX="8dp">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/accountNameET"
                style="@style/FullWidthMaterialEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textCapWords"
                android:nextFocusDown="@id/focusClearer"
                android:text="@={viewModel.accountNameETText}" />

            <View
                android:id="@+id/focusClearer"
                android:layout_width="match_parent"
                android:layout_height="0.01dp"
                android:focusable="true"
                android:focusableInTouchMode="true"
                app:layout_constraintTop_toBottomOf="@+id/accountNameET" />
        </com.google.android.material.textfield.TextInputLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/colourFamilyContainer"
            style="@style/FullWidthMaterialEditTextContainer.ExposedDropdownMenu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Colour"
            app:error_text="@{viewModel.colourFamilySpinnerError}"
            app:layout_constraintTop_toBottomOf="@+id/accountNameETContainer">

            <AutoCompleteTextView
                android:id="@+id/colourFamilySpinner"
                style="@style/FullWidthMaterialEditText.ExposedDropdownMenu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:dropDownAnchor="@id/accountIconFL"
                android:tag="colour family spinner"
                app:autoCompleteTextView_text="@={viewModel.colourFamilySpinnerText}"
                app:colourFamilySpinner_colourFamiliesAL="@{viewModel.colourFamilies}"
                app:colourFamilySpinner_colourIntensity="@{viewModel.colourIntensitySpinnerText}" />

        </com.google.android.material.textfield.TextInputLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/colourIntensityContainer"
            style="@style/FullWidthMaterialEditTextContainer.ExposedDropdownMenu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@{viewModel.colourIntensitySpinnerText == null ? `Colour Intensity (NA)` : `Colour Intensity`}"
            app:error_text="@{viewModel.colourIntensitySpinnerError}"
            app:layout_constraintTop_toBottomOf="@+id/colourFamilyContainer">

            <AutoCompleteTextView
                android:id="@+id/colourIntensitySpinner"
                style="@style/FullWidthMaterialEditText.ExposedDropdownMenu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:dropDownAnchor="@id/accountIconFL"
                android:dropDownHeight="@{viewModel.colourIntensitySpinnerText != null ? LP.WRAP_CONTENT : 0}"
                android:enabled="@{viewModel.colourIntensitySpinnerText != null}"
                android:tag="colour intensity spinner"
                app:autoCompleteTextView_text="@={viewModel.colourIntensitySpinnerText}"
                app:colourIntensitySpinner_colourFamily="@{viewModel.colourFamilySpinnerText}"
                app:colourIntensitySpinner_colourIntensitiesAL="@{viewModel.colourIntensities}" />

        </com.google.android.material.textfield.TextInputLayout>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
